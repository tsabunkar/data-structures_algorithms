/**
 * !Diameter of Binary Tree
 *
 * Given a binary tree, you need to compute the length of the diameter of the tree.
 * The diameter of a binary tree is the length of the longest path between any two
 *  nodes in a tree. This path may or may not pass through the root.
 *
 *        1
         / \
        2   3
       / \     
      4   5    
 * Return 3, which is the length of the path [4,2,1,3] or [5,2,1,3].
 *
 * Note: The length of path between two nodes is represented by the number of
 * edges between them.
 *
 */

// - identify the leaf nodes
// - traverse from one leaf node to another leaf node using shortest path
// - idenitifiy which is the longest path -> longestPath/Edge b/w two leaf nodes

/**
 * Definition for a binary tree node.
 * function TreeNode(val) {
 *     this.val = val;
 *     this.left = this.right = null;
 * }
 */

/**
 * @param {TreeNode} root
 * @return {number}
 */
/* 
var diameterOfBinaryTree = function (root) {

};
 */

/* function TreeNode(val) {
  this.val = val;
  this.left = this.right = null;
}

function BinaryTree() {
  this.root = null;
} */
/* 
BinaryTree.prototype.insert = function () {};
BinaryTree.prototype.remove = function () {};

BinaryTree.prototype.search = function () {}; */

/**
 * @param {TreeNode} root
 * @return {number}
 */
// BinaryTree.prototype.diameterOfBinaryTree = function (root) {};
