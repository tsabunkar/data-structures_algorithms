/**
 * Second implementation
 * @param {*} n : end number
 */
function addUpTo(n) {
    return n * (n + 1) / 2;
}

console.log(addUpTo(100));

/**
 * Now question arises which one is better solution out for the same problem ?
 */